web: bin/rails server
# watchman is optional — Tailwind falls back to polling if not installed.
# The stderr filter suppresses the harmless "watchman: command not found" warning.
css: bash -c 'bin/rails tailwindcss:watch 2> >(grep -v "watchman: command not found" >&2)'
